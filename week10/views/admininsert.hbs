<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">{{title}}</h1>

        <!-- Book 조회 및 삽입 -->
        <div class="mt-4">
            <h2>도서 목록 (Book)</h2>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>ISBN</th>
                        <th>Title</th>
                        <th>Year</th>
                        <th>Category</th>
                        <th>Price</th>
                        <th>Author_ID</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each books}}
                    <tr>
                        <td>{{this.ISBN}}</td>
                        <td>{{this.Title}}</td>
                        <td>{{this.Year}}</td>
                        <td>{{this.Category}}</td>
                        <td>{{this.Price}}</td>
                        <td>{{this.Author_ID}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            <h3>도서 데이터 삽입</h3>
            <form method="POST" action="/admin/insert">
                <input type="hidden" name="tableName" value="Book">
                <div class="row g-3">
                    <div class="col"><input type="text" name="ISBN" class="form-control" placeholder="ISBN" required></div>
                    <div class="col"><input type="text" name="Title" class="form-control" placeholder="Title" required></div>
                    <div class="col"><input type="number" name="Year" class="form-control" placeholder="Year" required></div>
                    <div class="col"><input type="text" name="Category" class="form-control" placeholder="Category" required></div>
                    <div class="col"><input type="text" name="Price" class="form-control" placeholder="Price" required></div>
                    <div class="col"><input type="number" name="Author_ID" class="form-control" placeholder="Author_ID" required></div>
                </div>
                <button type="submit" class="btn btn-primary mt-3">삽입</button>
            </form>
        </div>

        <!-- Author 조회 및 삽입 -->
        <div class="mt-4">
            <h2>저자 목록 (Author)</h2>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>URL</th>
                        <th>Address</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each authors}}
                    <tr>
                        <td>{{this.ID}}</td>
                        <td>{{this.Name}}</td>
                        <td>{{this.URL}}</td>
                        <td>{{this.Address}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            <h3>저자 데이터 삽입</h3>
            <form method="POST" action="/admin/insert">
                <input type="hidden" name="tableName" value="Author">
                <div class="row g-3">
                    <div class="col"><input type="number" name="ID" class="form-control" placeholder="ID" required></div>
                    <div class="col"><input type="text" name="Name" class="form-control" placeholder="Name" required></div>
                    <div class="col"><input type="text" name="URL" class="form-control" placeholder="URL" required></div>
                    <div class="col"><input type="text" name="Address" class="form-control" placeholder="Address" required></div>
                </div>
                <button type="submit" class="btn btn-primary mt-3">삽입</button>
            </form>
        </div>

        <!-- Award 조회 및 삽입 -->
        <div class="mt-4">
            <h2>수상 목록 (Award)</h2>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Year</th>
                        <th>Author_ID</th>
                        <th>Book ISBN</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each awards}}
                    <tr>
                        <td>{{this.ID}}</td>
                        <td>{{this.Name}}</td>
                        <td>{{this.Year}}</td>
                        <td>{{this.Author_ID}}</td>
                        <td>{{this.Book_ISBN}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            <h3>수상 데이터 삽입</h3>
            <form method="POST" action="/admin/insert">
                <input type="hidden" name="tableName" value="Award">
                <div class="row g-3">
                    <div class="col"><input type="number" name="ID" class="form-control" placeholder="ID" required></div>
                    <div class="col"><input type="text" name="Name" class="form-control" placeholder="Name" required></div>
                    <div class="col"><input type="number" name="Year" class="form-control" placeholder="Year" required></div>
                    <div class="col"><input type="number" name="Author_ID" class="form-control" placeholder="Author_ID" required></div>
                    <div class="col"><input type="text" name="Book_ISBN" class="form-control" placeholder="Book ISBN" required></div>
                </div>
                <button type="submit" class="btn btn-primary mt-3">삽입</button>
            </form>
        </div>

        <!-- Warehouse 조회 및 삽입 -->
        <div class="mt-4">
            <h2>창고 목록 (Warehouse)</h2>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Code</th>
                        <th>Address</th>
                        <th>Phone</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each warehouses}}
                    <tr>
                        <td>{{this.Code}}</td>
                        <td>{{this.Address}}</td>
                        <td>{{this.Phone}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            <h3>창고 데이터 삽입</h3>
            <form method="POST" action="/admin/insert">
                <input type="hidden" name="tableName" value="Warehouse">
                <div class="row g-3">
                    <div class="col"><input type="text" name="Code" class="form-control" placeholder="Code" required></div>
                    <div class="col"><input type="text" name="Address" class="form-control" placeholder="Address" required></div>
                    <div class="col"><input type="text" name="Phone" class="form-control" placeholder="Phone" required></div>
                </div>
                <button type="submit" class="btn btn-primary mt-3">삽입</button>
            </form>
        </div>

        <!-- Inventory 조회 및 삽입 -->
        <div class="mt-4">
            <h2>재고 목록 (Inventory)</h2>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Warehouse Code</th>
                        <th>Book ISBN</th>
                        <th>Number</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each inventories}}
                    <tr>
                        <td>{{this.Warehouse_Code}}</td>
                        <td>{{this.Book_ISBN}}</td>
                        <td>{{this.Number}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            <h3>재고 데이터 삽입</h3>
            <form method="POST" action="/admin/insert">
                <input type="hidden" name="tableName" value="Inventory">
                <div class="row g-3">
                    <div class="col"><input type="text" name="Warehouse_Code" class="form-control" placeholder="Warehouse Code" required></div>
                    <div class="col"><input type="text" name="Book_ISBN" class="form-control" placeholder="Book ISBN" required></div>
                    <div class="col"><input type="number" name="Number" class="form-control" placeholder="Number" required></div>
                </div>
                <button type="submit" class="btn btn-primary mt-3">삽입</button>
            </form>
        </div>

        <!-- Contains 조회 및 삽입 -->
        <div class="mt-4">
            <h2>장바구니 내용 (Contains)</h2>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Shopping Basket ID</th>
                        <th>Book ISBN</th>
                        <th>Number</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each contains}}
                    <tr>
                        <td>{{this.Shopping_basket_BasketID}}</td>
                        <td>{{this.Book_ISBN}}</td>
                        <td>{{this.Number}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            <h3>장바구니 데이터 삽입</h3>
            <form method="POST" action="/admin/insert">
                <input type="hidden" name="tableName" value="Contains">
                <div class="row g-3">
                    <div class="col"><input type="number" name="Shopping_basket_BasketID" class="form-control" placeholder="Shopping Basket ID" required></div>
                    <div class="col"><input type="text" name="Book_ISBN" class="form-control" placeholder="Book ISBN" required></div>
                    <div class="col"><input type="number" name="Number" class="form-control" placeholder="Number" required></div>
                </div>
                <button type="submit" class="btn btn-primary mt-3">삽입</button>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
